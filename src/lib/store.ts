import { writable } from 'svelte/store';

export const projectsVisible = writable(false);
export const resumeVisible = writable(false);
export const contactVisible = writable(false);
export const experienceVisible = writable(false);
